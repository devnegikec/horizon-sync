/**
 * Existing WMS batch activation workflow.
 *
 * This is intentionally kept separate from the new QSeal Activation page.
 * It continues to use the existing /batches APIs and component behavior.
 */
export { ActivationManagement as default, ActivationManagement as WMSActivationPage } from '../components/qseal/ActivationManagement';
